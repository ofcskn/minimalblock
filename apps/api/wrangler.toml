name = "minimalblock"
main = "../../dist/apps/api/worker.js"
compatibility_date = "2024-09-23"
compatibility_flags = ["nodejs_compat"]

[observability]
enabled = true

# ---------------------------------------------------------------------------
# Non-sensitive env vars — safe to commit, visible in wrangler.toml
# ---------------------------------------------------------------------------
[vars]
SUPABASE_URL = "https://pmzdxgndbzpctfkjtjdp.supabase.co"
TRENDYOL_MOCK = "false"

# ---------------------------------------------------------------------------
# Secrets — must be set via Cloudflare dashboard or `wrangler secret put`
# Do NOT commit real values here.
# Required secrets:
#   SUPABASE_SERVICE_ROLE_KEY
#   GEMINI_API_KEY
#   CORS_ORIGIN
#   TRENDYOL_MERCHANT_ID
#   TRENDYOL_API_KEY
#   TRENDYOL_API_SECRET
# ---------------------------------------------------------------------------

